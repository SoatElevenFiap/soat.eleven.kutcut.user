apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Release.Name }}
type: Opaque
stringData:
  ConnectionStrings__PostgresConnectionString: {{ .Values.secret.postgresConnectionString | quote }}
  JwtSettings__SecretKey: {{ .Values.secret.jwtSecretKey | quote }}