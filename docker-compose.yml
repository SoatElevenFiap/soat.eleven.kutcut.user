version: '3.8'

services:
  userdb:
    image: postgres:14
    restart: always
    environment:
      POSTGRES_USER: kutcut_user
      POSTGRES_PASSWORD: PiwSQpdLU2dSxwNS
      POSTGRES_DB: user_kutcut_db
    volumes:
      - db_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db_data:
    driver: local